<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * InventorySeeder is intentionally NOT called by default — it inserts
     * demo channels/products/stock and is meant to be run explicitly:
     *   php artisan db:seed --class="Database\Seeders\InventorySeeder"
     */
    public function run(): void
    {
        //
    }
}
